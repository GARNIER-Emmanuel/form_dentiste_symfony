<form method="post" action="{{ path('app_c_ontrolleur_info_client_crud_delete', {'id': info_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info_client.id) }}">
    <button class="btn">Delete</button>
</form>
